// Glorified JSON file - used just to get us comments, so use them!
// These will probably be configurable via the command line interface, but these
// are the defaults
module.exports = {

    // Enable/disable console coloring
    colors: true,

    // Enable/disable output
    quiet: false,

    // Enable/disable debug messages
    debug: false,

    // Enable/disable longer error messages
    verbose_errors: true,

    // Add files to the module search path
    module_paths: [],
    
};